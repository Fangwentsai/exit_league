# 分紙照片自動辨識 — 設計文件

> 狀態：規劃中（尚未實作）｜最後更新：2026/08

在 admin 上傳比賽結果的頁面，加入「拍照上傳分紙 → 自動填入 16 局資料 → 人工確認後送出」的功能。

---

## 為什麼這樣設計

這份設計不是憑空想的，是先拿真實分紙實測過才定案。以下幾個發現直接決定了架構：

### 發現 1：隊名欄不可靠，但這不重要

實測 10 張第六屆分紙，**3 張主隊或客隊欄整格空白、1 張被塗改難辨識**。四成有問題。

→ **結論：完全不要用 OCR 讀隊名。** 使用者在 admin 選了場次之後，系統從 Google Sheets 的 `schedule` 分頁就已經知道是哪兩隊，比讀照片可靠一百倍。這也讓辨識的難度下降一個量級（見發現 2）。

### 發現 2：辨識人名不需要「讀出」，只需要「認出是名單上的誰」

知道是哪兩隊之後，候選人就從 164 人縮小到約 20~38 人。OCR 就算把「小飛」讀成「小非」，模糊比對到封閉名單一樣能配對正確。

→ **結論：一定要把該場兩隊的名單當作候選清單傳給模型**，不要讓它自由發揮。

### 發現 3：分段小計驗算有盲點，不能全信

第六屆 G125 實測結果：

| 項目 | 準確率 |
|---|---|
| 比賽得分（21:9） | ✅ 100% |
| 飲酒加成 | ✅ 100% |
| 最終得分 | ✅ 100% |
| **逐局勝負** | **12/16（75%）** |

錯的是 SET7~10。關鍵在於：我判給主隊 SET6+7+8+9（1+1+1+1＝4 分），實際是主隊贏 SET6+SET10（1+3＝4 分）。**兩種完全不同的組合，分段小計一模一樣。**

→ **結論：總分核對只能驗證「總分對不對」，不能驗證「是哪幾局贏的」。** 逐局勝負必須有獨立的信心評估，不能因為總分對就放行。這種錯誤會默默污染個人勝場排行，而總分看起來完全正常。

### 發現 4：先攻本身讀得出來，但沒有任何驗算機制

**先攻的判讀經聯盟確認是正確的**——實測 8/4 分紙時，我一度懷疑有幾行「主先攻」與「主勝」同時被塗黑是螺旋筆畫滲到隔壁格，經確認並非如此：那些就是同一隊既先攻又獲勝，完全合理。所以先攻欄位是讀得出來的。

先前在**舊版分紙**上遇到的「數到 17 個先攻標記、超過 16 局」的矛盾，來源是舊版左右兩欄各記一次先攻、兩邊填法不一致。**新版分紙已把先攻改成中間欄只記一次，這個矛盾在結構上不可能再發生。**

不過仍有一項殘留風險：勝負至少有總分可以交叉檢查，**先攻完全沒有**。模型若讀錯，沒有任何機制會發現，而先攻直接餵給「地獄倒霉鬼」的先攻機率排行。

→ **結論：先攻可比照勝負套用信心分級，不需特別降級處理。** 但因為缺乏驗算機制，建議把它的自動填入門檻設得比勝負嚴格一些（例如 90% 而非 85%），作為補償。

### 發現 5：實際填法是螺旋塗鴉，不是塗滿

新分紙上寫「請完全塗滿方框」，但實測填寫者是在框裡畫螺旋。覆蓋率大約 40~60%，而不是接近 100%。

→ **結論：墨水覆蓋率的判定門檻要設在 15~20%，不是 70%。** 空白框的雜訊大約只有 0~2%，所以這個門檻仍能穩定區分。

### 發現 6：多人賽欄位的字跡明顯較差

單人局（SET1~9）欄位大、字工整，辨識率高；三人賽、雙人賽、四人賽的欄位小，字跡潦草許多。

→ **結論：信心分數應該預期在多人賽偏低，這是正常的，不是模型有問題。**

### 發現 7：通訊軟體轉傳會加浮水印

實測照片上有 LINE/微信轉傳時加的斜向浮水印，疊在塗黑框上會直接干擾覆蓋率判讀。

### 發現 8（重要）：Gemini 2.5 Flash 的信心分數目前沒有鑑別力，⚠️ 現行 85% 門檻不安全

用「人生揪難亮 vs 有點傻」熱身賽分紙（真實照片 + 真實正確答案，來自聯盟提供的 `熱身賽賽果.xlsx`）實測 `api/analyze-scoresheet.js`：

| 指標 | 數值 |
|---|---|
| 全部 64 欄位錯誤率 | 48.4%（容錯比對後）|
| **高信心（≥85）但答錯** | **29 格，佔全部欄位 45%** |
| 與 Claude 手動判讀同一批 16 個姓名欄位比較 | Claude 錯 31%／高信心錯 1 格；Gemini 錯 25%／高信心錯 **4 格** |

Gemini 的原始辨識率並不差（甚至略優於人工），**但信心分數幾乎不論對錯都落在 90~95**，沒有鑑別力。照目前的信心分數套用 85% 自動填入門檻，會有將近一半的自動填入結果是錯的，而且使用者在介面上看到的是「已確認」的綠色，沒有理由去複查。

**這不是隨機雜訊，找到兩個具體、可修的模式：**

1. **先攻／勝負答錯時 100% 是讀反**（先攻錯 7 次、勝負錯 9 次，全數為相反值，沒有第三種錯法）。代表模型在把塗黑框對應到正確的左右欄位這件事上有系統性問題，不是「看不懂」。
2. **姓名有「複製鄰列」的傾向**：正確答案中「宗燁」只出現在 SET2/8/12，但模型在同一視覺區塊裡的 SET1/3/4（單人局、字跡較潦草）也回傳「宗燁」。看起來像是認出附近某列清楚寫的名字後，套用到相鄰的模糊列，而不是誠實回報低信心——這正好違反了提示詞裡「不確定請回傳低信心，不要用猜的」的指示。

→ **結論：`SCORESHEET_OCR.md` 原訂的「85% 以上自動填入」門檻在目前的提示詞下不安全，暫緩套用於正式流程。** 下一步應該先针對上述兩個模式改善提示詞（例如要求模型先描述塗黑框相對 SET 標籤的左右位置、明確禁止套用鄰列答案），重新測過準確率與信心校準之後，才能重新評估自動填入門檻該設多少。在此之前，UI 端若要先上線，建議所有欄位一律視為「需人工確認」，不依賴模型信心分數分級。

→ **結論：上傳介面要明確要求「用相機原始照片，不要用聊天軟體存下來的版本」。**

### 發現 9：針對發現 8 的第一步修正——定位點透視校正已實作，但只用合成測試驗證過

發現 8 診斷出的第一個具體模式（先攻／勝負答錯 100% 是讀反）本質是空間判斷問題：照片是斜的、有透視變形，模型要自己在扭曲的畫面裡判斷「這個塗黑框算左邊還右邊」，出錯率自然高。

**修正方向：送給 Gemini 之前，先用分紙四角的定位點把照片校正回正的矩形**，讓塗黑框跟 SET 標籤的左右關係在畫面上就是清楚的垂直對齊，不需要模型自己做透視推論。

實作：`api/_lib/deskew.js`，流程是：

1. 在畫面四個象限裡找「中心暗、外圍亮」對比度最高的位置（定位點的視覺特徵——桌面是大片均勻的暗，不會有這種局部對比；紙上印的黑方塊才會）
2. 用找到的四個角點解一個四點透視變換（homography），把照片「拉」回固定比例的矩形畫布
3. 四個角只要有任何一個找不到（例如定位點被手指擋住、照片裁太緊），就放棄校正、照原圖送出，不假裝校正成功

已整合進 `api/analyze-scoresheet.js`：每次辨識請求都會先跑這一步，校正結果（`applied: true/false`）會附在回傳的 JSON 裡，方便之後追蹤校正成功率。

⚠️ **目前只用自己生成的合成測試圖驗證過**（拿 `pages/scoresheet.html` 的乾淨截圖套用已知的透視變形、疊加模擬桌面背景），確認校正後文字方向正確、checkbox 與 SET 標籤的左右關係清楚可辨識——這是合成測試能證明的部分。**還沒有用真實照片重新跑過發現 8 那份「人生揪難亮 vs 有點傻」的準確率測試**，所以校正是否真的改善了先攻／勝負的錯誤率，目前是「預期會改善」而不是「已證實改善」。同時針對發現 8 第二個模式（姓名複製鄰列），已在提示詞加入明確的信心分數判斷基準與出賽限制提示（同組 SET 不能出現同一人兩次），同樣還沒有真實照片的重新測試數據。

→ **下一步**：拿到真實照片後，重跑一次發現 8 的驗證方法論（同一份 `熱身賽賽果.xlsx` 正確答案），才能知道這幾項修正實際的效果，不能只憑合成測試或設計推論就當作已解決。

---

## 整體架構

```
使用者選擇場次（gamecode）
   ↓
系統已知：主隊、客隊 ← 來自 Google Sheets schedule 分頁
   ↓
顯示「上傳分紙照片」按鈕
   ↓
前端壓縮圖片 → POST /api/analyze-scoresheet
   ↓                （附帶兩隊名單當候選清單）
Vercel Serverless Function
   ├─ 從環境變數取得 GEMINI_API_KEY（金鑰永不進瀏覽器）
   └─ 呼叫 Gemini Vision API
   ↓
回傳結構化 JSON（每格附信心分數）
   ↓
前端依信心分級著色填入現有的 16 局表單
   ↓
使用者確認/修改 → 既有的驗證與上傳流程
```

**關鍵原則：這個功能只是「幫使用者把表單預先填好」，不是重寫 admin。** 第四步之後完全沿用現有流程。

---

## Step 1：取得 Gemini API Key

### 申請

1. 前往 [Google AI Studio](https://aistudio.google.com/apikey) 或 GCP Console
2. 建立 API key（免費方案即可，用量評估見下）
3. **不要**把 key 提交進 repo

### 用量評估

每週約 6 場比賽 × 1~2 張照片 = 每週 12 張以內。免費方案的速率限制（每分鐘請求數）與每日配額都遠高於這個量，**不需要付費方案**。

> ⚠️ 免費方案的資料可能被 Google 用於改進模型。對飛鏢分紙來說風險低，但要知情。

### ⚠️ 安全性：金鑰絕對不能放前端

`js/admin-main.js:120` 的登入是 `root` / `root666` **明碼寫在前端**，而這個 repo 是公開的。如果 API key 也照這樣放，等於公開你的 GCP 額度給任何人。

**必須走 Vercel Serverless Function**，repo 已有現成範例可以照抄：`api/shopee-products.js`（金鑰放 `process.env`，運算在伺服器端）。

環境變數設定（Vercel Dashboard → Settings → Environment Variables，三個環境都要勾）：

```
GEMINI_API_KEY=<你的金鑰>
```

程式端只寫 `process.env.GEMINI_API_KEY`，金鑰本身不會出現在 repo 的任何地方。

### 金鑰的可見範圍

| 位置 | 讀得到嗎 |
|---|---|
| 部署後的 `api/` 函式 | ✅ Vercel 執行時注入 |
| 前端 `js/*.js` | ❌ 永遠 `undefined`——**這正是我們要的** |
| 本機開發 | ❌ 除非自行建立 `.env.local` |

本機測試需在專案根目錄建立 `.env.local`（已在 `.gitignore` 內，不會被提交）：

```
GEMINI_API_KEY=<你的金鑰>
```

然後用 `vercel dev` 啟動，才能在本機呼叫到 `api/` 的函式。

---

## Step 2：上傳介面

### 觸發時機

在 `handleGameSelection()` 選定場次、`showGameDetails()` 顯示 16 局表單之後，於表單上方顯示上傳區塊。**未選場次前不顯示**——因為沒有 gamecode 就不知道是哪兩隊，辨識無從進行。

### 介面內容

```
┌────────────────────────────────────────────┐
│ 📷 上傳分紙照片自動填寫                      │
│                                            │
│ [ 選擇照片 ]  或直接拖曳到此處               │
│                                            │
│ ⚠️ 請用相機原始照片，不要用 LINE 轉傳的版本   │
│    （轉傳會加浮水印，影響辨識）              │
│ ⚠️ 請盡量正對紙面拍攝，四角的黑色方塊要入鏡    │
└────────────────────────────────────────────┘
```

### 前端處理

- 接受 `image/*`，單張
- 上傳前壓縮至長邊 ≤ 2000px、JPEG quality 0.85（降低傳輸時間與 API token 用量，這個解析度對辨識綽綽有餘）
- 顯示處理中狀態（Gemini 回應約需 5~15 秒）

---

## Step 3：由場次決定兩隊

**不讀照片上的日期與隊名**（見發現 1）。

`currentGame` 物件已經有 `.home`、`.away`、`.id`，直接使用：

```js
const homeTeam = currentGame.home;
const awayTeam = currentGame.away;
const homeRoster = allPlayers[homeTeam];   // 來自 data/player.json
const awayRoster = allPlayers[awayTeam];
```

> 💡 可選的防呆：若照片上的 gamecode 欄位有填寫且與所選場次不符，跳出提醒「照片場次為 G18，但你選的是 G20，確定要繼續嗎？」。這是額外的保護，不是主要判斷依據。

---

## Step 4：辨識與名單比對

### 送給 Serverless Function 的內容

```json
{
  "image": "<base64>",
  "gameCode": "G18",
  "homeTeam": "人生揪難亮",
  "awayTeam": "有點傻",
  "homeRoster": ["小姜", "亮亮", "紅閎", "..."],
  "awayRoster": ["小飛", "小齊", "歪歪", "..."]
}
```

### 提示詞要點

給 Gemini 的指示必須包含：

1. **分紙的版面結構**：左欄主隊選手、中間四個塗黑框（主先攻／主勝／客勝／客先攻）、右欄客隊選手，共 16 局
2. **每局的人數**：SET1~4、6~9 單人；SET5、10 三人；SET11~14 雙人；SET15、16 四人
3. **候選名單**：「主隊選手只可能是這份清單裡的人：…」「客隊選手只可能是：…」
4. **塗黑框的填法可能是螺旋塗鴉而非塗滿**，有明顯筆跡即視為已勾選
5. **要求每個欄位輸出 0~100 的信心分數**
6. **不確定時輸出 null 並給低信心分數，不要猜**

### 期望的回傳格式

```json
{
  "sets": [
    {
      "set": 1,
      "homePlayers": [{ "name": "亮亮", "confidence": 92 }],
      "awayPlayers": [{ "name": "小安", "confidence": 71 }],
      "firstAttack": { "value": "home", "confidence": 88 },
      "winner":      { "value": "home", "confidence": 95 }
    }
  ],
  "drinkingBonus": { "value": "home", "confidence": 60 },
  "scores": { "home": 21, "away": 9, "confidence": 90 }
}
```

### 後處理：名單模糊比對

模型回傳的名字仍要在**前端或 serverless 端**再做一次比對，不能直接信任：

```js
// 若模型回傳的名字不在名單內，找最接近的
// 建議用 Levenshtein 距離；完全對不上就降為低信心
function matchRoster(name, roster) {
    if (roster.includes(name)) return { name, penalty: 0 };
    const best = closestByEditDistance(name, roster);
    return best.distance <= 1
        ? { name: best.name, penalty: 15 }   // 小幅扣信心
        : { name: null, penalty: 100 };      // 對不上，強制人工
}
```

### 交叉驗算（有限度地用）

若模型也讀出了底部的「比賽得分」，可以與逐局勝負推算的結果比對：

```js
const computed = sumPoints(winners);  // SET_POINTS = [0,1,1,1,1,3,1,1,1,1,3,2,2,2,2,4,4]
if (computed.home !== ocr.scores.home) {
    // 總分對不上 → 一定有局讀錯，整批勝負降為需確認
}
```

> ⚠️ **反過來不成立**：總分對得上**不代表**每局都對（見發現 3）。這個檢查只能用來「發現錯誤」，不能用來「提升信心」。

---

## Step 5：信心分級與 UI

> ⚠️ **以下分級規則目前不能直接套用。** 實測發現 Gemini 2.5 Flash 的信心分數幾乎不論答對答錯都落在 90~95（見發現 8），高信心答錯的比例高達 45%。下表是**目標設計**，要等提示詞改善、信心分數重新校準後才能真的拿信心數字當自動填入的依據。在那之前，所有欄位一律走「黃色／需人工確認」的路徑，不依賴模型自報的信心值分級。

### 分級規則（目標，尚未可用，見上方警告）

| 信心 | 顏色 | 行為 |
|---|---|---|
| **≥ 85%** | 綠色／正常 | 自動填入，**使用者仍可修改** |
| **50~84%** | 🟡 黃色 | 自動填入但標記為未確認，**必須逐一點擊確認才能送出** |
| **< 50%** | 🔴 紅色 | 不填入，**必須人工選擇** |
| 空值 / 對不上名單 | 🔴 紅色 | 同上 |

> 💡 **先攻的門檻建議設為 90%**（而非 85%）。先攻讀得出來（見發現 4），但它沒有任何交叉驗算機制，模型讀錯不會被任何檢查攔下來，用較嚴格的門檻補償。這個建議本身沒變，但前提是先解決發現 8 的信心校準問題。

### 送出前的檢查（三層都要通過）

1. **不能留空** — 沿用既有的 `validateGameData()`，它已經檢查 16 局的主客選手、先攻、勝負、飲酒加成
2. **黃色欄位全部已確認** — 新增檢查：不能有 `pending-confirm` 狀態的欄位
3. **出賽限制** — 沿用既有的 `checkPlayerConflict()` 與 `noRepeatGroups`（SET1~4、6~9、11~12、13~14 各組內不得重複）

> 💡 **既有程式碼已經有 `noRepeatGroups` 和 `checkPlayerConflict()`**（`js/admin-main.js:466` 附近），不需要重寫，只要在自動填入後跑一次全域檢查。

### 需要新增的狀態

現有的表單狀態變數（`selectedPlayers`、`firstAttackData`、`winLoseData`、`bonusTeam`）維持不變，另外新增一份平行的信心狀態：

```js
// key 格式與現有變數一致，例如 'home-1'、'firstAttack-1'、'winLose-1'
const ocrConfidence = {};   // { 'home-1': 92, ... }
const ocrPending   = {};    // { 'away-3': true }  黃色未確認
```

這樣設計的好處：**OCR 是可選的輔助**，不用時完全不影響原有流程；使用者手動改過的欄位直接清掉對應的 pending 狀態即可。

### 視覺呈現

- 黃色欄位：底色標黃 + 右上角小圓點，點擊後圓點消失、底色轉正常
- 紅色欄位：底色標紅 + 「請選擇」字樣，行為與現在的空欄位一致
- 頂部顯示彙總：`⚠️ 尚有 5 項需確認、2 項待填寫`
- 提供「全部確認」按鈕（給信心普遍偏高、快速核對完的情況）

---

## 實作清單

| # | 項目 | 檔案 | 相依 |
|---|---|---|---|
| 1 | 申請 Gemini API key、設定 Vercel 環境變數 | — | — |
| 2 | Serverless function | `api/analyze-scoresheet.js`（新增，照抄 `api/shopee-products.js` 的結構） | 1 |
| 3 | 上傳 UI 區塊 | `pages/admin.html` | — |
| 4 | 圖片壓縮 + 呼叫 API | `js/admin-ocr.js`（新增） | 2, 3 |
| 5 | 名單模糊比對 | `js/admin-ocr.js` | 4 |
| 6 | 信心狀態與著色 | `js/admin-ocr.js` + `js/admin-main.js` | 5 |
| 7 | 送出前的三層檢查 | `js/admin-main.js`（擴充 `validateGameData()`） | 6 |
| 8 | 實測校準信心門檻 | — | 7 |

**建議順序**：先做 1、2，用一張真實照片測辨識品質，**確認可行再做 3~7**。UI 是最耗工的部分，不值得在驗證可行性之前投入。

---

## 未定事項

0. **（最優先）如何讓信心分數真的可信？** 見發現 8——目前 45% 的欄位是「高信心但答錯」，這件事不解決，其他所有分級規則都是空談。已做的：定位點透視校正（見發現 9，已實作但只用合成測試驗證）、提示詞加入信心分數判斷基準與出賽限制提示。還沒做的候選方向：
   - 加入第二次獨立詢問，兩次答案一致才視為高信心（self-consistency），不單純採信模型自報的數字
   - 塗黑框改走純影像處理（見未定事項 2），至少讓先攻／勝負這兩類欄位不再依賴模型的空間判斷
   - **用真實照片重跑發現 8 的驗證方法論**——目前發現 9 的修正都只有合成測試佐證，這一步是確認「有沒有真的變好」的關鍵，不能跳過

1. **先攻的自動填入門檻要設多少？** 已確認先攻讀得出來（見發現 4），可套用信心分級。但因為它缺乏交叉驗算機制，建議門檻比勝負嚴格（勝負 85%、先攻 90%）。實際數字待未定事項 0 解決、信心分數校準後再定。

2. **要不要做純影像處理的塗黑框判讀？** 塗黑框的判讀（有塗／沒塗）其實不需要 LLM——用四角定位點校正後，直接量各框的墨水覆蓋率即可，**免費、不受模型能力影響、比 LLM 更可靠**。實測發現先攻／勝負這類「空間判斷」正是 Gemini 目前最弱的環節（見發現 8），這個方向的優先度因此提高：可以考慮塗黑框走影像處理、人名走 Gemini，兩者分開處理各自的強項。

3. **admin 的登入安全性** — 目前是前端明碼 `root`/`root666`。這個功能會讓 admin 承擔更多自動化職責，建議一併改成後端驗證（見 `HEALTH_CHECK.md` 第 1 項）。

4. **退件與鎖定機制（保底機制，尚未設計）** — 目的：防止上傳者故意亂填（例如客隊 SET16 完全亂寫）騙過辨識流程。構想是「同一場比賽累積兩次判定失敗，就鎖定該場的上傳分析功能」，但實際設計還有幾個待決定的問題：
   - **「判定失敗」的具體標準是什麼？** 候選：整體信心平均低於某個門檻、特定欄位完全對不上名單（`notInRoster`）、`crossCheck.sumValid` 為 false、`repeatViolations` 非空——這幾種是不同性質的錯誤，要嘛選一種當標準，要嘛加權組合，需要先看更多真實案例才能定門檻。
   - **失敗次數要存在哪裡？** Vercel serverless function 是無狀態的，這個專案目前唯一的持久層是 Google Sheets（透過 Apps Script Web App 寫入）。比照現有模式，應該是在 Sheets 開一個欄位或分頁記錄「gamecode → 失敗次數」，需要擴充 GAS 的寫入動作。
   - **鎖定後誰能解鎖？** 需要 admin 手動解鎖的介面，否則使用者卡住無法上傳分紙（退回人工填寫仍然可行，鎖定的只有「自動辨識」這個輔助功能，不影響手動填寫）。
   - 這一項排在未定事項 0（信心分數校準）之後——鎖定機制要判斷「是不是刻意亂填」，前提是信心分數本身要先可信，否則會把「模型自己讀錯」誤判成「使用者亂填」。

### 已排除

- ~~**先攻／勝欄位滲色**~~ — 曾懷疑螺旋筆畫會滲到隔壁格，經聯盟確認為誤判：兩欄同時塗黑的那幾行是同隊既先攻又獲勝，屬正常情形。分紙的欄位間距不需調整。

---

## 預期效果與風險

**預期**：把人工輸入從「16 局 × 2 隊全填」降到「確認少數黃色欄位」，估計省下 7~8 成的輸入時間。

**最大風險**：不是模型不夠聰明，而是——

- 記錄員沒有確實填塗黑框（實測已遇到整張全空的分紙）
- 照片經通訊軟體轉傳帶浮水印
- 多人賽欄位字跡潦草

前兩項是流程問題，用再好的模型都救不回來。**建議在推廣這個功能的同時，一併向各隊長說明分紙的填寫要求。**

---

## 相關檔案

| 檔案 | 用途 |
|---|---|
| `pages/scoresheet.html` | 分紙本體（可列印），已針對辨識最佳化 |
| `pages/admin.html` | 上傳介面 |
| `js/admin-main.js` | 16 局表單、選手選擇、驗證、上傳 |
| `data/player.json` | 當季各隊名單（比對用的候選清單） |
| `api/shopee-products.js` | Serverless function 的參考範例 |
| `HEALTH_CHECK.md` | 安全性待辦（admin 密碼、API key） |
