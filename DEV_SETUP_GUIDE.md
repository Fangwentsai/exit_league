# 開發環境設定指南

## 🎯 目的
建立獨立的開發測試環境 (dev.yhdarts.com)，用於開發新功能和後台系統，不影響正式環境。

## 📋 環境說明

### 正式環境
- **網址**: https://yhdarts.com
- **分支**: main
- **用途**: 正式對外服務

### 開發環境
- **網址**: https://dev.yhdarts.com
- **分支**: dev
- **用途**: 開發測試、新功能驗證

## 🚀 設定步驟

### 1. DNS 設定
在你的 DNS 管理介面（如 Cloudflare）添加 CNAME 記錄：

```
類型: CNAME
名稱: dev
內容: fangwentsai.github.io (或你的 GitHub Pages 網址)
代理狀態: 僅限 DNS (橘色雲朵關閉)
```

### 2. GitHub Pages 設定
1. 進入 GitHub Repository Settings
2. 找到 "Pages" 設定
3. 在 "Build and deployment" 部分：
   - Source: Deploy from a branch
   - Branch: 選擇 `dev` 分支
   - Folder: / (root)
4. 儲存設定

### 3. 等待部署
- GitHub Pages 需要幾分鐘時間部署
- 部署完成後訪問 https://dev.yhdarts.com

## 💻 開發流程

### 切換到開發環境
```bash
git checkout dev
```

### 開發新功能
```bash
# 在 dev 分支進行開發
# 修改檔案...
git add .
git commit -m "新功能: XXX"
git push origin dev
```

### 測試完成後合併到正式環境
```bash
# 切換到 main 分支
git checkout main

# 合併 dev 分支的變更
git merge dev

# 推送到正式環境
git push origin main
```

### 從正式環境同步最新變更
```bash
# 在 dev 分支
git checkout dev

# 從 main 拉取最新變更
git merge main

# 推送更新
git push origin dev
```

## 📁 目錄結構

```
exit_league/
├── dev/                    # 開發環境專用目錄
│   ├── README.md          # 開發環境說明
│   └── admin/             # 新後台系統
│       ├── index.html     # 後台首頁
│       ├── styles/        # 樣式檔案
│       └── js/            # JavaScript 檔案
├── pages/                 # 正式環境頁面
├── js/                    # 正式環境 JS
└── ...
```

## 🔧 後台系統開發

新的後台系統位於 `dev/admin/` 目錄：

- **網址**: https://dev.yhdarts.com/dev/admin/
- **功能**: 比賽管理、選手管理、數據統計等
- **技術**: HTML + CSS + JavaScript

## ⚠️ 注意事項

1. **不要在 dev 分支修改正式環境的核心檔案**
   - 如需修改，應該在 main 分支進行，然後 merge 到 dev

2. **測試完成才 merge 到 main**
   - 確保功能正常運作
   - 沒有破壞性變更

3. **定期同步**
   - 定期從 main 同步最新變更到 dev
   - 避免分支差異過大

4. **CNAME 檔案**
   - main 分支: `yhdarts.com`
   - dev 分支: `dev.yhdarts.com`

## 🎨 開發環境特色

- 頂部有明顯的 "🚧 開發環境" 標示
- 獨立的後台管理系統
- 可以隨意測試，不影響正式站

## 📞 問題排查

### 無法訪問 dev.yhdarts.com
1. 檢查 DNS 設定是否正確
2. 檢查 GitHub Pages 是否已部署 dev 分支
3. 等待 DNS 傳播（最多 24 小時）

### 變更沒有生效
1. 確認已推送到 GitHub
2. 等待 GitHub Pages 重新部署（約 1-2 分鐘）
3. 清除瀏覽器快取

## 🎉 完成！

現在你有兩個獨立的環境：
- ✅ 正式環境: yhdarts.com (main 分支)
- ✅ 開發環境: dev.yhdarts.com (dev 分支)

可以放心在開發環境測試新功能了！


